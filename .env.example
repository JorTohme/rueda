# Used by the API and database scripts. Keep real credentials in .env or your deployment secret store.
DATABASE_URL=postgresql://fleet:fleet@127.0.0.1:5433/fleet_saas
PORT=3002
# Development-only demo owner credentials. Change them before sharing the environment.
DEMO_EMAIL=demo@rueda.local
DEMO_PASSWORD=demo-password
